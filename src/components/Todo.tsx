const Todo = () => {
  return (
    <div>
      <p>Add Watch live Section</p>
      <p>
        Add Chat Section to Room (users watching live matching would be able to
        interact)
      </p>
      <p>to add online/offline functionality</p>
      <p>
        Consider the case when A send_request to B and then leaves the game. Now
        B accepts the req and is waiting in the lobby for A. (Only start the
        game when both A & B are online)
      </p>
    </div>
  );
};

export default Todo;
